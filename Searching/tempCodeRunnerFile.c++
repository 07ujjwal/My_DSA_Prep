  while (low <= high) {
    //        int mid = (low + high) / 2;
    //        int partitions = countPartitions(nums, mid);
    //       if (partitions > k) {
    //         low = mid + 1;
    //       }
    //        else {
    //          high = mid - 1;
    //       }
    //    }
    // return low;